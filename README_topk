in result_topk folder, contains all results generated by calling script_topk.m (call sample_topk.m)

After get all raw results, stacking1_topk.py is to generate stacking results

Then read_comp_avg.py includes three part:
  1. copy stacking result to correct position
  2. compute avg of stacking of kronls result
  3. plot best one across fold number
easily comment/uncomment these three one by one to obtain final figures
